import React from 'react';
import { Container, Header } from '@pages/Channel/styles';

const Channel=()=>{
    return(
            <Container>
                <Header>로그인 하신 것을 축하드려요!</Header>
            </Container>
    );
}

export default Channel;